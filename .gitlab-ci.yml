image: archlinux:latest

pages:
  stage: deploy
  script:
    - pacman -Syu --noconfirm kotlin
    - ./scripts/ci.main.kts --highlight "Highlights - Demo" --targetDir "public" --targetUrl "https://kirchewds.gitlab.io/kirchenfuehrung-data/"
  artifacts:
    paths:
      - public
    expire_in: 2 days
  only:
    - master
